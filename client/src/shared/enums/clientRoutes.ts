export enum CLIENT_ROUTES {
  MAIN = "/",
  BOOKS = "/books",
  ADDBOOK = "/addbook",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  PROFILE = "/profile",
  EDITING = "/editing",
  ABOUT = "/about",
  NOT_FOUND = "*",
}
