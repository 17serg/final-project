export enum CLIENT_ROUTES {
  MAIN = "/",
  BOOKS = "/books",
  ADDBOOK = "/addbook",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  PROFILE = "/profile",
  ABOUT = "/about",
  NOT_FOUND = "*",
  SIGN_UP_TRENER = "/signuptrener"
}
